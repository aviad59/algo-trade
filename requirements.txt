# Runtime dependencies (mirrors [project.dependencies] in pyproject.toml).
# Preferred install: pip install -e ".[dev]"  — this file exists for
# tooling and graders that expect a requirements.txt.
edgartools
pydantic>=2.0
anthropic>=0.92
pandas>=2.0
matplotlib>=3.8
fastapi>=0.115
uvicorn[standard]>=0.32
python-dotenv>=1.0
tqdm>=4.66
