# Course-submission convenience file. The authoritative dependency spec is
# pyproject.toml; the recommended install is still:
#
#     pip install -e ".[dev]"
#
# This file mirrors the runtime + backtest dependencies for a plain:
#
#     pip install -r requirements.txt
#
# (installing this way does NOT create the `filingsignal` console script;
# use `python -m filingsignal.cli ...` or the editable install above.)

pydantic>=2.0
pyyaml>=6.0
python-dotenv>=1.0
pandas>=2.0
numpy>=1.26
edgartools>=4.0
anthropic>=0.69
openai>=1.50
fastapi>=0.115
uvicorn[standard]>=0.32
yfinance>=0.2

# --- dev / test extras ---
pytest>=8.0
httpx>=0.27
