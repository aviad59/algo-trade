# --- secrets: never commit ---
.env
.env.local
*.key

# --- python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
build/
dist/

# --- node / frontend build ---
frontend/node_modules/
frontend/dist/
node_modules/
*.tsbuildinfo

# --- sqlite sidecars (the buffer itself IS committed; its WAL is not) ---
*.sqlite-wal
*.sqlite-shm

# --- local noise ---
server.log
*.log
.DS_Store

# --- presentation film: renders and its own toolchain live on disk only ---
# 700+ MB of Remotion node_modules and a 73 MB 1080p render. The film is a
# deliverable in its own right, not part of the application.
video/
