# Copy to .env.local to override. Defaults work with `filingsignal serve` on :8000.
VITE_API_BASE=/api/v1
# "api" (default) hydrates from the backend; "fixtures" forces demo data.
VITE_DATA_SOURCE=api
