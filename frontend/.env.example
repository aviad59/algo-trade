# Base path for forecast JSON (static mock or live API)
VITE_API_BASE=/mock/v1

# mock | api — mock uses static JSON; api calls the FastAPI backend
VITE_DATA_SOURCE=mock

# Live API example (requires `algo-trade-api` on port 8000):
# VITE_API_BASE=/api/v1
# VITE_DATA_SOURCE=api
